{
  "name": "toe-cursor-control",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.3.5",
    "vitest": "^3.2.1",
    "fast-check": "^4.1.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18"
  }
}
